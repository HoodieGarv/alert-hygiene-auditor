"""Database layer — SQLAlchemy models, session factory, and schema migrations.

Submodules
----------
models  : ORM table definitions (AlertFiring, IngestionRun).
session : Thread-safe session context manager backed by the configured engine.
"""
